# 文档索引

> 本项目所有设计类文档都在本目录，**按编号顺序阅读**。编号 = 建议阅读顺序，不代表写作时间。
> 最后更新：**2026-09-14** —— 补入 `10` 与 `11` 的索引条目，并更正 `09` 的过期状态（体检发现，依据 `11-项目体检报告.md` §3.5）。

---

## 顺序一览

| 编号 | 文件 | 类型 | 一句话 | 状态 |
|---|---|---|---|---|
| **01** | `01-产品设计文档.md` | 基线 | 产品定位、目标用户、MVP v1 范围、路线图 | 已确认（部分内容已过期，见 `11` §3.5） |
| **02** | `02-体验改进建议-第一篇.md` | 评审 | 功能闭环能不能用：25 条 P0/P1/P2 | 已落地大部分 |
| **03** | `03-客户体验与界面改造建议-第二篇.md` | 评审 | 新体验问题（E-xx）+ 界面为什么不好看（V-xx） | 已落地大部分 |
| **04** | `04-界面逐屏标注-第三篇-需求与生成页.md` | 评审 | 基于实际截图的像素级逐块标注（P-01～P-13） | 依据 |
| **05** | `05-功能与界面交互设计.md` | **设计** | 功能地图 + 页面结构 + 交互流程 + 状态与文案规范 | 流程基线（头部"待确认"字样待回填，见 `11` P1-8） |
| **06** | `06-界面设计规范.md` | 施工图 | 色板 / 字号 / 间距 / 组件的**唯一取值来源** | 生效中 |
| **07** | `07-开发进度与交接.md` | 交接 | 代码结构 / 接口速查 / 运行命令 / **踩坑 33 条** / 已实现与未实现 / **数据在哪** | 生效中（测试数字已过期） |
| **08** | `08-后端架构设计.md` | **设计** | 服务化后端：数据库（16 张表）/ Redis / 中间件栈 / API 路由 / P0–P3 路线 | 已确认（决策见 §13） |
| **09** | `09-后端实现计划.md` | **计划** | P0 存储层 / P1 API 层 / P2 生产化，每步含文件、验证命令与出口标准 | P0 完成、**P1-1…P1-7 完成**；**P2 建议暂停**（见 `11` §4.4） |
| **10** | `10-全天菜单设计.md` | **设计** | 全天菜单（早/午/晚可勾选）：餐次模型 → 内核按餐分池 → 界面 → 数据库 | 已完成 7 步 |
| **11** | `11-项目体检报告.md` | **体检/评审** | 用户视角 × 项目经理视角：实测证据、P0–P2 改进清单、风险登记册、建议暂停项 | 待你过目 |

另有一份项目备忘在**工作区根目录**（不属于本目录）：`D:\cook\聊天小结-recipe-planner项目.md`（求职定位、选型、环境、简历讲法）。

---

## 怎么读（按你的目的选）

**要动手改界面 / 写页面**
→ 先读 **05**（有哪些功能、页面怎么分、交互怎么走）→ 再按 **06** 取值（颜色字号间距，不要另起一套）→ 需要依据时回查 04。

**要动后端 / 数据库 / 服务化**
→ 读 **08**（选型、16 张表、中间件顺序、API 路由、P0–P3 路线与验收）→ 再读 **09**（做到哪一步了、每步怎么验）→ 再读 **07** §1.5「数据在哪」+ §2 代码结构与 §6 踩坑 → 落地时遵守 08 §7 的"同名适配层"约定（界面与现有测试不改）。
→ **但先看 `11` §4.4**：P2（Postgres / Redis / 可观测）目前建议暂停。

**想快速了解这个产品**
→ 读 **01**（是什么、给谁、做到哪）→ 再读 **05** 的 §1 功能地图与 §2 页面结构。

**想知道"为什么界面是这样改的"**
→ **02**（功能闭环 → 为什么加清单打勾/持久化/放宽条件）→ **03**（视觉诊断 → 为什么收颜色、去 emoji）→ **04**（像素证据 → 为什么锁浅色主题、去掉 Deploy）。

**要验收**
→ **05** §8 体验验收清单（8 条）+ **06** §8 界面自查清单（10 条）+ **11** §6 的测试门禁口径。

**要做体检 / 复盘投入产出 / 决定下一步做什么**
→ 读 **11**：实测证据（附录 A）、P0 正确性 6 条、P1 一致性 8 条、风险登记册、建议暂停项。

---

## 维护约定

1. **06 是样式的唯一来源**：任何新页面都必须从 `06` 取色板、字号、间距、组件规格，不得另起一套。
2. **05 是功能与流程的基线**：功能增删、页面结构变化，先改 `05` 再动实现；`05` 与 `06` 冲突时，**流程以 05 为准、取值以 06 为准**。
3. **新增文档按编号追加**（下一个空号是 `12-…`），不要插队改号；评审类文档只增不改，结论变化写进新的设计文档。
4. **08 是后端基线**：数据库表、中间件、API 路由的改动先改 `08` 再动实现；数据层改造必须保持 `07` §4 的接口签名。
5. **测试/进度数字只留一处**：对外口径统一为「pytest + `self_check` + `smoke_app` + `smoke_api_app`」，具体数字以实测输出为准，不在多份文档里各写一份（这是 `11` P1-7/P1-8 的教训）。

---

## 改名对照（2026-09-14 文档目录重排）

| 原名 | 现名 |
|---|---|
| `产品设计文档.md` | `01-产品设计文档.md` |
| `MVP到正式版-体验改进建议.md` | `02-体验改进建议-第一篇.md` |
| `客户体验与界面改造建议-第二篇.md` | `03-客户体验与界面改造建议-第二篇.md` |
| `界面逐屏标注-第三篇-需求与生成页.md` | `04-界面逐屏标注-第三篇-需求与生成页.md` |
| （新增） | `05-功能与界面交互设计.md` |
| `界面设计规范.md` | `06-界面设计规范.md` |
| （后续新增，未改名） | `07-开发进度与交接.md`、`08-后端架构设计.md`、`09-后端实现计划.md`、`10-全天菜单设计.md`、`11-项目体检报告.md` |

文中所有互相引用的路径、以及 `app.py` 顶部注释里的文档路径，均已同步更新为编号名。
