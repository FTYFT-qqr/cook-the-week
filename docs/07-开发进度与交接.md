# 开发进度与交接（截至 2026-09-11）

> 编号 **07** ｜ 类型：**交接/进度** ｜ 上游：`05-功能与界面交互设计.md`（流程基线）、`06-界面设计规范.md`（样式取值）
> 用途：**换人/换会话接手时，只读本篇 + `05` + `06` 即可继续开发**，不必回看聊天记录。

---

## 1. 一句话现状

一个「一周晚餐规划」Agent：填需求 → LLM 排菜 + **确定性校验** → 出菜单与买菜清单；
界面按 `05` 做成**以「今晚」为中心**的五页应用，结果可持久化、改动单点生效、可上下班式地天天用。
**功能闭环与界面改造已完成**，**后端 P0（存储层：SQLite + 仓储 + Alembic 迁移）与 P1-1/P1-2
（FastAPI 骨架 + 中间件 + 只读接口）也已落地**，且可一键回退到 JSON；
测试 **163 + 165（JSON）/ 161 + 161（DB）+ 60（pytest）** 全绿，本机 demo 可跑。

---

## 1.5 数据在哪（后端 P0，2026-09-11）

**默认 `STORAGE=db`：数据在 `data/app.db`（SQLite，WAL）。** 想回到 JSON 文件，一条环境变量即可：

```powershell
$env:STORAGE='json'    # 回退：读 data/recipes.json + saved_plans.json + customer_profile.json
```

| 项目 | 说明 |
|---|---|
| 建库/升级 | `python -m recipe_planner.storage.migrate`（空库 `upgrade head`；`create_all` 建的老库自动 `stamp head`，**不动数据**） |
| 迁移 | `alembic upgrade head` / `alembic downgrade base`；基线 `0001_init`（16 张表） |
| JSON → DB | `python -m recipe_planner.storage.import_json`（先备份到 `data/backup/<时间戳>/`，可重复执行） |
| 等价校验 | `python scripts/verify_migration.py` → 18 项（菜谱逐字段、方案逐天逐菜、档案集合、摘要重算） |
| 保留策略 | 超过 12 周的老方案自动归档（`PLAN_RETENTION_WEEKS`，只标记不删除） |
| 备份 | 迁移前自动备份 JSON；数据库本体备份在 P2（`docs/09`） |

> 数据库文件不进 git（`.gitignore` 已排除 `data/app.db*`、`data/backup/`、`logs/`、`.tmp/`）。

### 1.6 API 服务（P1）

```powershell
& $py -m uvicorn recipe_planner.api.main:app --host 127.0.0.1 --port 8000
```

| 接口 | 说明 |
|---|---|
| `GET /health` · `GET /ready` | 存活 / 就绪（就绪逐项报告 db、redis、llm，DB 不通返回 503） |
| `GET /api/v1/recipes` | 菜谱库（`q` / `category` / `liked=all\|true\|false` / `limit` / `cursor`） |
| `GET /api/v1/plans` | 以前的方案（最新在前，带 `current_id`） |
| `GET /api/v1/plans/current` | **今晚页**：状态①–⑤ + 今晚菜/时间/金额/理由/几点能吃上（没有方案也 200） |
| `GET /api/v1/plans/{id}` | 方案详情（7 天 + 每日菜 + 清单 + 概览 + 结构统计） |
| `GET /api/v1/plans/{id}/days/{day}` | 单天（含下锅顺序） |
| `GET /api/v1/profile` | 档案（喜欢/不喜欢/评分/来源痕迹/指纹） |
| `GET /docs` | OpenAPI 文档（可交互） |

错误统一 `application/problem+json`：`{code,message,request_id,status,details.next_steps}`，
`message` 一律人话，`next_steps` 给可点击的下一步（05 §5.1）。写入类接口在 P1-3。

---

## 2. 代码结构

| 文件 | 职责 |
|---|---|
| `app.py` | Streamlit 界面（约 1600 行）：五页 + 动作页、样式表（设计 token）、会话状态、交互处理 |
| `recipe_planner/models.py` | pydantic 模型：`Recipe/Ingredient/UserConstraints/DayPlan/ChosenDish/ValidationIssue/ShoppingItem/PlanResult` |
| `recipe_planner/core.py` | **确定性核心**：检索过滤、排菜、校验、修正、清单、单道替换、人话改菜（省钱/换菜/跳天/补天） |
| `recipe_planner/graph.py` | LangGraph 编排：`retrieve → plan(LLM) → validate ⇄ repair → shopping → answer`（`MAX_REPAIRS=2`） |
| `recipe_planner/llm_planner.py` | DeepSeek 调用（严格 JSON、temperature=0 保证可复现）、失败一律降级不抛错 |
| `recipe_planner/assistant.py` | 「一句话改菜单」解析引擎（规则 + LLM 兜底）。**界面按 05 §1.4 未开放自由对话**，引擎保留并被测试覆盖 |
| `recipe_planner/reporting.py` | 客户语言层：概览摘要、下锅顺序、结构统计、CSV/文本/A4 打印、分享视图、批次拆分 |
| `recipe_planner/store.py` | 方案存档：`data/saved_plans.json`（最近 3 份），含勾选进度 `checked_items`、已完成日 `done_days` |
| `recipe_planner/profile.py` | 口味档案：喜欢/不喜欢、来源痕迹 `history`、评分 `ratings` |
| `recipe_planner/progress.py` | 生成过程的阶段反馈与**可取消**（后台线程 + `graph.stream`） |
| `recipe_planner/ui_state.py` | 会话级记忆：操作回执、多步撤销栈(5)、改动历史、二次确认、页面状态键 |
| `recipe_planner/db.py` / `data/recipes.json` | 菜谱库（**30 道**，字段完整；**无做法步骤 `steps`**，所以「下锅顺序」只用 `time_min`+`category` 推） |
| `recipe_planner/infra/settings.py` | 运行开关集中地：`STORAGE / USE_API / AUTH_MODE / DATABASE_URL / REDIS_URL / PLAN_RETENTION_WEEKS / LLM_MODEL` |
| `recipe_planner/storage/orm.py` | 16 张表声明式模型（`docs/08` §3.2 的 DDL） |
| `recipe_planner/storage/engine.py` | async engine/session；SQLite 打开 WAL + busy_timeout + 外键 |
| `recipe_planner/storage/repositories.py` | `RecipeRepo / PlanRepo / ProfileRepo / LogRepo`（async，只进出领域模型） |
| `recipe_planner/storage/sync_bridge.py` | 常驻事件循环线程，让同步的界面/测试能调 async 仓储 |
| `recipe_planner/storage/db_store.py`·`db_profile.py` | 与 `store.py`/`profile.py` **同名同义**的数据库实现（`STORAGE=db` 时接管） |
| `recipe_planner/storage/migrate.py` | 建库/升级入口（`upgrade head` / 老库 `stamp head` / 幂等） |
| `recipe_planner/storage/import_json.py` | JSON → DB 一次性导入（先备份，可重复执行） |
| `recipe_planner/storage/async_adapters.py` | **服务端**按 `STORAGE` 分派的 async 数据入口（路由只能用这个，不能用同步门面） |
| `recipe_planner/tonight.py` | 「今晚」状态①–⑤ 的判定与文案（纯函数，界面与 API 共用；`app.py` 那份 P1-7 删） |
| `recipe_planner/infra/logging.py` | 一行一条 JSON 日志 + `request_id` 贯穿（stdout + `logs/app.log` 轮转 14 天） |
| `recipe_planner/api/main.py` | FastAPI 应用工厂（中间件顺序 + 路由挂载），`uvicorn recipe_planner.api.main:app` |
| `recipe_planner/api/middleware/` | `request_id.py` / `access_log.py` / `session.py`（P1-4 再加 auth/ratelimit/idempotency） |
| `recipe_planner/api/errors.py` · `deps.py` · `schemas.py` | `problem+json` 统一错误 / 依赖注入 / 接口 DTO |
| `recipe_planner/api/routes/` | `health.py`（`/health`、`/ready`）、`recipes.py`、`plans.py`（含 `/plans/current` 今晚数据）、`profile.py` |
| `alembic/` + `alembic.ini` | 迁移（基线 `0001_init`），连接串由 `settings.database_url()` 提供 |
| `scripts/self_check.py` | 离线自测 **163 项**（JSON）/ **161 项**（DB）（确定性核心、存档、报告、阶段反馈、解析引擎、收尾功能） |
| `scripts/smoke_app.py` | AppTest **UI 回归 165 项**（JSON）/ **161 项**（`SMOKE_STORAGE=db`） |
| `scripts/verify_migration.py` | JSON 与数据库的**等价校验 18 项**（菜谱逐字段、每份方案逐天逐菜、档案集合） |
| `scripts/smoke_llm.py` | 真实 DeepSeek 端到端冒烟（约 3s，校验 0 问题） |
| `tests/` | `pytest` **60 项**：建表/约束/级联、迁移往返、迁移与 ORM 一致、中间件、只读接口契约、今晚状态机 |

---

## 3. 页面结构与交互（05 基线）

左栏任务栏 **5 项**，默认落在「今晚」：

| 页 | 要点 |
|---|---|
| **今晚**（默认首页） | 主角大卡（`今晚 · 周三 9/17` / 菜名 22px / `约 45 分钟 · 预计 ¥62` / 一句理由 / 可选「几点能吃上」）+ 每道菜一行`这道不吃` + 两个整卡级快改`回家晚了`/`来客人了` + `做完了`/`开始做饭`；**五种状态**：①有安排 ②已跳过 ③已做过（含打分）④还没有菜单（引导卡）⑤这一周已结束 |
| **本周计划** | 顶部方案名+`重排一版`/`改需求`；`哪里能省`/`都满意`/`分享视图`；7 个等宽天行（今天高亮、已过只读、已跳过显示`不做饭`）；每日详情（逐道卡 + `换一道`/`定住`/`喜欢`/`不喜欢` + 下锅顺序）；`以前的方案`（切版/照上一版/删除） |
| **排一周** | 三张场景卡 + 四组表单（人数与天数 / 口味与忌口 / 时间与预算 / 家里已有）+ `生成菜单`；阶段反馈可取消；首次排完落「今晚」、重排落「本周计划」 |
| **买菜清单** | 到店模式（一行一样、点一下打勾、已买沉底变淡、勾选**写进存档**）+ `分两次买`（周初耐放/周中新鲜）+ 单菜小料标`可选` + `带走清单`（CSV / 文本 / A4 打印） |
| **口味档案** | 喜欢/不喜欢两个可编辑列表 + 来源痕迹（`8/10 在菜单里点的`）+ 就地说明 + `导出档案`/`清空`/隐私说明 |

**八条不变量（05 §4）**均已落地：单点改动只影响单点、动作有回执且可撤销、破坏性操作二次确认、
改不了就给可点击的下一步、结果可复现（temperature=0）、忌口永不妥协并明说"已避开"、数字都带口径、状态永远可见。

---

## 4. 关键接口速查

- 约束：`UserConstraints(people, days, dishes_per_day, allergens, spice_level, taste_tags, goal, max_time_min, skill, cook_start, budget_per_person_day, pantry_items, liked_dishes, disliked_dishes, must_include_recipes)`
- 计划：`DayPlan(day, meal, dishes, skipped, people)`（`people` =  "来客人了" 的单日份量）
- 单点操作：`swap_dish / replace_in_day / skip_day / restore_day / fastest_day / cheapest_swap / protein_swap / veg_swap`
- 词匹配换菜：`recipes_matching / candidates_matching`；刷新：`refresh_result`
- 存档：`store.save_plan / update_result / previous_record / delete_record / set_done / set_checked / today_index`
- 档案：`prof.set_feedback(name, action, known, source=) / rate(name, score) / feedback_origin / rating_of`
- 报告：`rep.plan_summary / day_minutes / cook_order / structure_line / shopping_csv / shopping_text / printable_html / share_text / split_batches`

---

## 5. 运行与验证命令

```powershell
# 环境（conda 环境在 D:\conda\cook\recipe-planner，直接调 python.exe）
$py = 'D:\conda\cook\recipe-planner\python.exe'

# —— 数据（默认 STORAGE=db；这两条命令幂等，可随时重跑）——
& $py -m recipe_planner.storage.migrate          # 建库/升级到 head
& $py -m recipe_planner.storage.import_json      # 仅在需要从 JSON 重新导入时用
& $py scripts\verify_migration.py                # JSON 与 DB 等价校验 18 项

# —— 回归（两种存储后端都要绿）——
& $py -m pytest tests -q                         # 60 项
$env:STORAGE='json'; & $py scripts\self_check.py # 163 项
$env:STORAGE='db';   & $py scripts\self_check.py # 161 项
& $py scripts\smoke_app.py                       # 165 项（AppTest，默认 JSON）
$env:SMOKE_STORAGE='db'; & $py scripts\smoke_app.py   # 161 项（DB，自动用临时库）

# —— API 服务（P1）——
& $py -m uvicorn recipe_planner.api.main:app --host 127.0.0.1 --port 8000
# 验证：/health、/ready、/docs（OpenAPI）、/api/v1/plans/current

# —— 真实 LLM 冒烟 ——
$env:DEEPSEEK_API_KEY='<key>'; & $py scripts\smoke_llm.py

# 起服务（工作目录必须是 D:\cook\recipe-planner）
$env:DEEPSEEK_API_KEY='<key>'
& $py -m streamlit run app.py --server.headless true --server.port 8501 --server.address 127.0.0.1
# 验证：http://127.0.0.1:8501 与 /_stcore/health
```
密钥：`.env`（`DEEPSEEK_API_KEY`、`OPENAI_BASE_URL=https://api.deepseek.com`、`LLM_MODEL=deepseek-chat`）。
测试用的环境变量：`RECIPE_PROFILE_FILE`、`RECIPE_PLAN_FILE`（指向临时文件，避免污染真实档案）；
**DB 模式下这两个变量不生效**，测试改用 `DATABASE_URL` 指向临时库。
注意：`self_check.py` 的开关是 `STORAGE`，`smoke_app.py` 的开关是 `SMOKE_STORAGE`（它还要顺带换一份临时库）。

---

## 6. **踩过的坑（接手前务必看）**

1. **绝不要用 PowerShell 改源码**：`Get-Content -Raw` + `Set-Content` 会按 GBK 解码 UTF-8，**毁掉中文**（本项目已因此损坏过 `app.py`，靠 `git checkout` 才救回）。文本改动一律用 edit/write 工具。
2. **改 `app.py` 前先停 Streamlit**，否则写入报 `ReplaceFileW EIO (Win32 1175)`；**启动前确认 8501 空闲**，否则 `Port 8501 is not available` 而你以为服务在跑（旧的 200 可能来自上一个进程）。
3. **AppTest 的坑**：*元素引用在 rerun 后失效*（每次重新取）；`at.session_state` **没有 `.get()`**（用 `"k" in at.session_state`）；checkbox 用 `.check()`；按钮的 `type` 是字符串；同页多个 toggle 要**按 key 过滤**再操作；`st.progress/st.toast/st.code` 可能不被 element_tree 解析（断言要宽容）。
4. **widget key 不能在实例化之后写**：需要程序化改控件值时统一挂 `session_state["pending_sync"]`，脚本开头再 `_sync_widgets_from_inputs`。
5. **勾选/清空的 key 用 epoch**：`chk_{epoch}_{name}`，清空 = `epoch += 1`（不要直接改已实例化的 widget key）。
6. `data/recipes.json` 里 `调料/其他` 类食材不进采购清单；份量按 `people/2` 折算（菜谱是 2 人份基准）。
7. **`alembic.ini` 必须是纯 ASCII**：Alembic 用**系统 locale 编码**读这个文件，本机是 GBK，写中文注释会直接 `UnicodeDecodeError` 起不来（其它文件不受影响，正常写中文）。
8. **本机沙箱下 `os.mkdir(path, 0o700)` 建出的目录连列都列不了**（`WinError 5`），而 `tempfile.mkdtemp` 把 `0o700` 写死了 → Streamlit/AppTest 的临时目录建完就用不了、退出时也删不掉，`atexit` 打出一串 `PermissionError`（**看着像回归失败，其实断言早全过了**）。`scripts/smoke_app.py` 开头已把 `mkdtemp` 换成"默认权限"版本修掉它。历史遗留的 `\.tmp\tmp*` 目录在本会话内删不掉（`takeown`/`icacls`/`rd` 全被拒），只能在本沙箱之外删。
9. **别在数据库线程里调同步门面**：`sync_bridge.run()` 从数据库线程再提交协程会死锁（已加显式报错拦住）。导入时算档案指纹用的是同 session 内的 `_profile_sig_in_session`，不要改回去。
10. SQLite 的 `ON DELETE CASCADE` **默认不生效**，必须靠 `attach_sqlite_pragmas()` 打开 `foreign_keys=ON`；生产 engine 和测试都要挂。
11. **中间件顺序（Starlette 反直觉）**：`add_middleware` 是**后加的在最外层**；而注册的**异常处理器在最内层**。
    后果：路由抛出的领域异常会被就地转成 4xx/5xx 响应，外层的 Session 中间件**根本看不到异常** ——
    所以它必须**按响应码判定**（≥400 回滚、否则提交），不然"出错的请求"照样会被提交。
12. **路由里不要调 `store.*` / `profile.*` / `db.load_db()` 这些同步门面**：它们内部走 `sync_bridge.run()`，
    而请求本身就在事件循环里，再往里塞协程会直接报错。服务端一律用 `storage/async_adapters.py`。
13. **pytest 抓不到我们自己的日志**：`setup_logging()` 的 handler 在 import 期就绑定了当时的 `sys.stdout`，
    `capsys` 换掉 `sys.stdout` 之后它照样往老对象写（`capfd` 也未必灵）。
    要断言日志就**自己挂一个 handler、在 `emit` 里立刻格式化**（`tests/api/test_middleware.py` 的 `_JsonCollector`）。
14. **测 500 的响应体要 `httpx.ASGITransport(app=..., raise_app_exceptions=False)`**，
    否则异常会被抛到测试里、看不到 problem+json。代价是真崩溃也会变成 500，所以每条断言都要写清状态码。
15. `profile` 的 `ratings` 形状是 `{菜名: {"score": 0|1|2, "date": "MM/DD"}}`（由 `profile.rate()` 定），
    不是 `{菜名: 2}`；`history` 里是 `{"since": "MM/DD", "source": "在哪点的"}`。写测试数据时别再猜。

---

## 7. 与文档的关系（避免再次冲突）

- `05` 是**流程基线**：功能与页面结构以它为准（`05` 与 `06` 冲突时，**流程听 05、取值听 06**）。
- `02/03/04` 是**评审文档，只增不改**；结论变化写进新的设计文档。
- **已发生的冲突与裁定**：
  1. `02` 第一篇 E2 要"自由对话改需求"，`05` §1.4 **明确不做** → 界面不提供自由输入，引擎留在 `assistant.py` 并被测试覆盖；
  2. 左栏导航：`05` §2.2 原定 4 项、`排一周` 降级为动作页 → **2026-09-11 产品负责人决定加回任务栏第 3 项**（共 5 项），`05` 已加「变更记录」。

---

## 8. 已实现（按文档编号）

- **02 第一篇**：A1 持久化+回访、D1 人话指标、D2 整周总览、F2 清单带走（CSV/文本/A4）、F3 采购打勾、G1 响应式、H1 放宽选项、E3 二次确认+多步撤销、C1 阶段反馈+取消、J1 边界措辞、B1 周预算、B4 已有食材标签输入、F1 拆批+可选标注、D4 厨艺、B2 开饭倒推、I2 来源痕迹、I3 导出/隐私、E-08 分享视图（属 03 但同源）
- **03 第二篇**：E-01 认识今天、E-02 定住/加一道、E-03 临时情况、E-04 都满意、E-05 省钱提示、E-06 结构统计、E-07 做完打分、E-08 分享、E-10 工程词替换；V-01～V-16 全部（含 A4 打印、浅色锁定、emoji 退出、响应式一套组件）
- **04 第三篇**：P-01～P-13 全部
- **05**：M1～M5、八条不变量、§5 状态与文案规范

---

## 9. 未实现（下一批建议，按性价比排序）

1. **B2 工作日/周末分档**：现在只有"开饭倒推"；分档需要按天不同的时长上限，要动排菜核心
2. **E4 "不喜欢"分级**：区分「这周不想看到」与「以后都不要」（档案结构加时间维度）
3. **E-09 越用越懂你的证据**：如"最近 3 周你标了 5 道快手菜，所以工作日菜越来越短"
4. **A2 方案历史对比视图**：现在只有"切到这份 / 照上一版"；对比与"照上周来一份"的显式入口可加强
5. **P2 大项**：按人忌口与家庭多成员（B3/E5）、使用回顾与成就（I4）、首访引导（E-11）
6. **能力类**：向量检索升级、精确营养计算、天气/菜价、账号与多端同步（`01` 已声明不做，除非改基线）
