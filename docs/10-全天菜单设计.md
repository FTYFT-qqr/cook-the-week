# 10 · 全天菜单（可选餐次）设计

> 编号 **10** ｜ 类型：**设计基线** ｜ 上游：`05`（流程基线）、`06`（样式取值）、`08`（后端架构）、`09`（实现计划）
> 起因：产品负责人的原话是「修改为全天的菜单，然后增加菜品数量」。本文件把这句话变成可实现的约定。
> 结论：**餐次是配置项**（用户勾选吃哪几顿，默认只勾晚餐），不是硬编码的三餐。

---

## 1. 为什么这么做

现在产品是「一周**晚餐**规划」：`models.MEAL = "晚餐"`、`MEALS_PER_DAY = 1`，
`DayPlan` 一天只有一条，`UserConstraints.dishes_per_day` 是"每顿几道菜"。

需求要的是"全天"，但**不能硬编码三餐**：

1. 真实家庭的情况不一样 —— 有人只在家吃早餐、有人只做晚饭、有人三餐都做。
   产品负责人的原话是「根据用户情况实际选择，比如用户可以选择只做早餐或者只做晚餐」。
2. 硬编码三餐会让**不勾早餐的用户**也背上多一顿的复杂度（数据、界面、导出全要变）。
3. 更重要的是：**默认只勾晚餐 = 现有行为一个字都不变**，于是 165（smoke）+ 161/163（self_check）
   + 192（pytest）这三套回归全部继续有效 —— 这是这个设计最大的价值，不是顺带的好处。

## 2. 定下来的取舍（产品负责人已确认）

| 项 | 决定 |
|---|---|
| 餐次 | `早餐 / 午餐 / 晚餐` 三个复选，**默认只勾晚餐**；勾了才排、才进清单 |
| 每餐道数 | 各自一个数，默认 **早 1 / 午 2 / 晚 3**，范围 1–5（原来只有 2 道、上限 4 ——"增加菜品数量"落地在这里） |
| 早餐时长 | **默认 15 分钟**，表单可调 5–30（存进 `breakfast_max_time_min`）；午/晚沿用现有"单菜 ≤ N 分钟" |
| 早餐时长口径 | 粥类按"**≤15 分钟能上桌**"记（用高压锅/预约），**不引入 `hands_on_min` 新字段** —— 多一个字段就要多一套校验与展示，收益不抵成本 |
| 预算 | `budget_per_person_day` 口径**不变 = 每人每天**，覆盖当天勾选的所有餐；表单字段不动，只改提示文案 |
| 默认看哪顿 | 「今晚」页默认看当天勾选的**最后一顿**；只勾晚餐时 = 现在完全一样 |
| 页名 | 只勾晚餐时保持「今晚」；多餐时显示「今天」并在顶部给餐次切换 |
| 早餐菜谱 | 库里 0 道早餐，补 **8 道**（见 §4） |

## 3. 三种做法与选型

**A（采用）：餐次是配置项，`plan_day` 变成"一天一顿"**
- `UserConstraints.meals` 是 `早/午/晚` 的子集，默认 `["晚餐"]`；
- 数据库里 `plan_day`（现在一天一行）改成**一天一行每一顿**，
  `skipped` / `done_at` / `people_override` 自然变成"这一顿"的属性 —— "今天跳过午餐"才说得通。

**B（放弃）：只在 `plan_dish` 上加 `meal` 列，`plan_day` 仍一天一行**
改动小一点，但 `skipped` / `done` / `people` 还是"一天"级 —— "只跳过午餐"表达不了，
界面会出现说不清的状态。**省下的迁移成本，会变成界面上说不清的语义**，不划算。

**C（放弃）：三餐做成三份独立方案**
不动模型，但购物清单要跨方案合并（现在是一份内部合并）、「今晚」页要先选哪一份、
「以前的方案」列表翻三倍 —— 把复杂度推给界面，而且"一天的总花费/总时长"就没人算了。

## 4. 数据模型

### 4.1 常量与约束（`models.py`）

```python
MEALS = ("早餐", "午餐", "晚餐")
MEAL = "晚餐"                      # 保留：老数据/老代码的默认值
MEAL_DISH_DEFAULTS = {"早餐": 1, "午餐": 2, "晚餐": 3}
BREAKFAST_MAX_TIME_DEFAULT = 15    # 早餐时长上限的默认值（实际取 constraints.breakfast_max_time_min）
DISH_MIN, DISH_MAX = 1, 5          # 每餐道数范围（上限从 4 提到 5）
```

### 4.2 `UserConstraints` 的兼容策略（**一处规则，一个地方**）

新增 `meals: list[str] = ["晚餐"]`、`dishes_per_meal: dict[str, int] = {}`，
**保留** `dishes_per_day: int = 2`（老方案、老表单、老接口都在用它）。访问规则只写在方法里：

```python
def active_meals(self) -> list[str]:
    """这一周要排哪几顿，按 早→午→晚 的顺序；空/非法则回落到 ['晚餐']。"""
    picked = [m for m in MEALS if m in set(self.meals or [])]
    return picked or [MEAL]

def dishes_for(self, meal: str) -> int:
    """这一顿几道菜：dishes_per_meal 优先，没写就用 dishes_per_day（老数据兼容）。"""
    return int(self.dishes_per_meal.get(meal) or self.dishes_per_day)

def time_cap_for(self, meal: str) -> int:
    """这一顿单道菜的时长上限：早餐有自己的上限（默认 15 分钟），午/晚用 max_time_min。"""
    if meal == "早餐":
        return int(self.breakfast_max_time_min)
    return int(self.max_time_min)
```

新增字段一览（都带默认值，所以**老数据不用迁移**）：

| 字段 | 默认 | 含义 |
|---|---|---|
| `meals` | `["晚餐"]` | 这一周排哪几顿 |
| `dishes_per_meal` | `{}` | 每餐几道菜；空则回落到 `dishes_per_day` |
| `breakfast_max_time_min` | `15` | 早餐单菜时长上限（ge=5, le=30） |

> 为什么允许两个字段并存：老方案（JSON/DB 里已有 3 份）里只有 `dishes_per_day`。
> 与其写一个数据迁移去猜"晚餐几道"，不如让**读取时**有一条明确的回落规则，
> 且这条规则只出现在 `dishes_for()` 里（`docs/07` §6 第 25 条的教训：两处实现迟早漂）。

### 4.3 `DayPlan`：一天一顿

`DayPlan` 已经在 `models.py` 里有 `meal` 字段（P0 留的口子），现在真正用起来：

- **同一天会出现多条 `DayPlan`**，靠 `(day, meal)` 唯一确定；
- 于是 `next((p for p in result.days if p.day == X))` 这种写法会**静默拿到第一顿** ——
  这是本轮最大的地雷。所有查找必须走 `PlanResult` 上的两个方法：
  ```python
  def slots_for(self, day: int) -> list[DayPlan]:   # 这一天所有餐，按 MEALS 顺序
  def slot(self, day: int, meal: str | None = None) -> DayPlan | None:  # 指定餐；None=最后一顿
  ```
- `DayPlan.skipped` / `people` 的语义收紧为"**这一顿**不做饭 / 这一顿几个人"。

### 4.4 `PlanRecord.done_days` → `done_slots`

"做完了"从"这一天"变成"这一顿"：

- 新增 `done_slots: list[str]`，元素形如 `"3|晚餐"`（`slot_key(day, meal)`）；
- **保留** `done_days: list[int]`：老存档里它表示"当天所有餐都做过了"，
  读的时候由 `record.is_done(day, meal)` 合并判断，写的时候一律只写 `done_slots`。

## 5. 排菜与校验（第②步的落点，先记口径）

- **候选池按餐分**：早餐池 = 分类「早餐」+「主食」+ 蛋类菜品；午/晚池 = 现有的热菜/凉菜/汤/主食。
- **结构规则按餐各跑一遍**：现有"第 1 道偏主菜（含蛋白），第 2 道起补蔬菜/汤"是**按天内的位置**做的
  （`core.py:138-150`），改成按 `(天, 餐)` 各跑一遍即可，规则本身不动。
- **时长**：早餐用 `time_cap_for("早餐")=15`，午/晚用 `max_time_min`。
- **预算**：仍按天汇总（覆盖当天所有餐），超预算的提示口径不变。
- **清单**：本来就是跨天合并的，加餐不增加复杂度；条目上补"用于：午餐、晚餐"。
- **LLM 提示词**：`每天恰好选 N 道菜` → 按餐分别给 N 与"必须是早餐"的约束（`llm_planner.py:78,118`）。

## 6. 界面（第③步的落点）

| 页面 | 改动 |
|---|---|
| 排一周 | 新增「吃哪几顿」三个复选（默认只勾晚餐）+ 每餐道数；早餐的时长上限用 15 分钟 |
| 本周计划 | 每天从"一行"变成"按餐分段"：早餐 1 道、午餐 2 道、晚餐 3 道各一段；上方的整周总览按餐累加 |
| 今晚 | 默认看最后一顿；多餐时页名「今天」+ 顶部餐次切换（只勾晚餐时与现在完全一致） |
| 买菜清单 | 条目上加"用于：午餐、晚餐"；合并与勾选机制不变 |
| 口味档案 | 不变（菜还是那些菜） |

## 7. 接口与数据库（第④步的落点）

- **DTO**：`DayOut` 增加 `meals: list[MealOut]`（每餐的菜/时长/金额/是否做过）；
  `dishes` 保留为"所有餐拍平"，**老客户端不坏**。`ConstraintsOut`/`PlanCreateIn` 增加 `meals` 与 `dishes_per_meal`。
- **数据库**：Alembic `0002` —— `plan_day` 加 `meal VARCHAR`，唯一键改 `(plan_id, day_no, meal)`，
  **已有行填 `'晚餐'`**；`plan_dish` / `shopping_*` 不动（它们挂在 `plan_day` 上）。
- **迁移等价校验**：`scripts/verify_migration.py` 要能覆盖"老库升级后仍是 3 份方案、9 天、18 道菜"。

## 8. 分四步做（每步一个提交 + 一次验证，中间随时能跑）

| 步 | 内容 | 出口标准 |
|---|---|---|
| **①** | 数据模型（`meals` / `dishes_per_meal` / `PlanResult.slot*` / `done_slots`）+ 8 道早餐菜谱 + 「早餐」分类 | **回归零变化**：pytest 192、smoke 165/161、self_check 163/161 全绿（因为默认只勾晚餐，本步是纯增量）；新增模型单测 |
| **②** | 排菜内核：按餐分池、结构规则按餐跑、时长/预算口径、LLM 提示词、清单"用于" | 新增"勾三餐"的内核用例；默认路径回归不变 |
| **③** | 界面：表单勾餐次与每餐道数、本周计划按餐分段、今天页餐次切换 | `smoke_app.py` 仍 165 全绿 + 新增多餐用例 |
| **④** | API/DTO + Alembic `0002` + 导出按餐分段 + 客户端同步 | `verify_migration` 通过；`smoke_api_app.py` 扩展后全绿；真机只读核对 |

## 9. 不做的事（YAGNI）

- **不做 `hands_on_min`（动手时间）**：为了粥类多一个字段，收益不抵成本（§2 已定）。
- **不做"午餐带饭"**：用户要的是可选餐次，带饭是另一种产品形态（多做一份装盒），先不做。
- **不做按餐分预算**：口径保持"每人每天"，需要时再加。
- **不做餐次的排序/自定义名称**：固定早/午/晚三种，顺序固定。
- **不改菜谱库的既有权重**：早餐只加新菜，不动现有 30 道的任何字段。

## 10. 风险与对策

| 风险 | 对策 |
|---|---|
| `p.day == X` 的旧写法静默取到第一顿 | 第①步就把 `PlanResult.slot/slots_for` 建好，并在测试里断言"两条同 day 不同 meal"的查找；第②步逐个改调用点 |
| 老数据（3 份方案）没有 `meals` / `dishes_per_meal` | 读取回落规则只在 `active_meals()` / `dishes_for()` 里；`verify_migration` 覆盖 |
| 早餐池太小（8 道）导致一周重复 | 早餐 1 道 + 8 道池 ≈ 一周不重样；`校验` 里现有的"重复菜"规则会兜住 |
| 一次改动面太大（模型+内核+界面+接口+迁移） | 分四步，每步都能跑、都有独立出口标准（§8） |

---

## 11. 实施进度

### 第①步 ✅ 数据模型 + 早餐菜谱（2026-09-13）

**做了什么**（全部是纯增量，默认只勾晚餐 → 不勾早餐的用户一个字都不受影响）

- `models.py`：`MEALS` / `MEAL_DISH_DEFAULTS` / `BREAKFAST_MAX_TIME_DEFAULT` / `DISH_MIN,DISH_MAX`；
  `UserConstraints` 增加 `meals`（默认 `["晚餐"]`）、`dishes_per_meal`（默认 `{}`）、
  `breakfast_max_time_min`（默认 15，范围 5–30），并把这三种读取规则**只写在三个方法里**：
  `active_meals()` / `dishes_for(meal)` / `time_cap_for(meal)`。
  老字段 `dishes_per_day` 保留：`dishes_per_meal` 没写时回落到它 —— 这是"老方案不用迁移"的实现方式。
- `PlanResult.slots_for(day)` / `slot(day, meal=None)`：**取 DayPlan 一律走这两个方法**。
  一天多顿之后 `next(p for p in result.days if p.day == X)` 会静默拿到早餐，
  这是本轮最容易犯、最难在界面上看出来的错。
- `PlanRecord.done_slots`（`"3|晚餐"`）+ `is_done(day, meal)`：老存档的 `done_days=[3]`
  当成"那天所有餐都做过了"，同样不做数据迁移。`done_meals_of(day)` 给界面标"已做过"。
- 顺手清掉两处陈旧代码：`MEALS_PER_DAY`（写死 1、已无人引用）、`DayPlan.recipe_ids` 的**重复定义**。
- `core.recipe_conflicts` 增加一条硬过滤：**早餐菜只在勾了早餐时才是候选**。
  没有它，"只做晚餐"的用户会被排出一碗皮蛋瘦肉粥当晚饭 —— 这条也是"回归零变化"的前提。
- `data/recipes.json`：追加 8 道早餐（`r31`–`r38`，全部 ≤15 分钟，分类「早餐」）：
  皮蛋瘦肉粥、小米南瓜粥、番茄鸡蛋面、葱花鸡蛋饼、白煮蛋配黄瓜、燕麦牛奶、吐司煎蛋、香煎速冻饺子。
  按"一行一道菜"的原排版追加，**diff 只多 8 行**（整份重排会让 30 道菜的 diff 全出现，评审时看不出改了什么）。

**验证（出口标准 = 回归零变化）**

| 套件 | 结果 |
|---|---|
| `pytest tests` | **218 项**（原 192 + 新增 26）全绿 |
| `self_check.py` | JSON **163** / DB **161** —— 与改动前**完全一致** |
| `smoke_app.py` | JSON **165** / DB **161** —— 与改动前**完全一致** |

新增 `tests/test_meals_model.py`（26 项）：餐次读取规则与非法值回落、每餐道数回落与上限 5、
早餐时长上限与范围校验、**同一天多顿的查找**（守住那个地雷）、"做过了"两种记法共存、
早餐池守卫（只做晚餐时 8 道早餐一道都进不了候选）。

**下一步（第②步）**：排菜内核按餐分池 —— 结构规则（第一道主菜、第二道起蔬菜/汤）按 `(天, 餐)` 各跑一遍、
早餐用 `time_cap_for("早餐")`、预算仍按天汇总、LLM 提示词按餐给道数、买菜清单条目补"用于哪几餐"。

### 第②步 ✅ 排菜内核按餐分池（2026-09-13）

**做了什么**

- `core.in_meal_pool(r, meal)`：**全项目唯一的**"这道菜属于哪一顿"判定
  （早餐池 = 分类「早餐」+「主食」+ 主料含蛋的菜；其余餐排除早餐菜）。含蛋看**食材名**，不猜菜名。
- `recipe_conflicts(r, c, meal=None)` / `retrieve_candidates(db, c, meal=None)`：给了 `meal` 就按
  那一顿的池子与时长上限判（早餐默认 15 分钟）；**不给 `meal` 时行为与之前完全一致** → 老调用方一行没改。
- `pool_for_meal(candidates, c, meal)`：把 graph 给的候选并集切成"这一顿能用的菜"，复用同一个 `recipe_conflicts`。
- `plan_deterministic`：改成逐天**逐顿**排。每顿用自己的池子与道数；结构规则（第一道偏主菜、
  第二道起补蔬菜/汤）**按餐各跑一遍**（规则本身没改）；**预算仍是"每人每天"，跨餐累加** ——
  不能每顿各花一份预算。
- `validate_plan`：时长上限按餐取；预算**先把当天各餐加起来**（否则"每人每天 ¥45"会被当成"每顿 ¥45"，
  预算校验形同虚设）；文案多顿时带餐次（`第2天早餐`），**单顿时一字不变**（smoke/self_check 上有断言压着）。
- `llm_planner`：多顿时按餐给道数、要求输出 `meal` 字段，缺一顿或道数不对就**整体判不通过**
  （宁可回退到确定性结果，也不留半张全天菜单）；**单顿时提示词与之前一字不差**。

**顺手堵掉一颗地雷**：`core` 里有 **7 处** `next(p for p in plans if p.day == X)`，一天多顿之后它们会
**静默拿到早餐** —— "今晚换一道"会去改早上的粥，界面上完全看不出来（正是 §10 预判的那条）。
新增 `slot_of(plans, day, meal=None)`（None = 当天最后一顿）与 `with_slot()` 之后全部替换；
`skip_day` / `restore_day` / `swap_dish` / `fastest_day` / `pick_replacement` / `candidates_matching` /
`_fits_day_budget` / `replace_in_day` 都加了可选的 `meal` 参数（不传 = 当天最后一顿，单顿时与之前一致）。

**验证**

| 套件 | 结果 |
|---|---|
| `pytest tests` | **237 项**（第②步新增 19）全绿 |
| `self_check.py` | JSON **163** / DB **161** —— 与改动前**完全一致** |
| `smoke_app.py` | JSON **165** / DB **161** —— 与改动前**完全一致** |
| `smoke_api_app.py` | **44** 全绿 |

新增 `tests/test_meals_plan.py`（19 项）：勾三餐每天三段且顺序正确、只勾晚餐与之前一致、
只做早餐也成立、早餐只吃早餐池、一周不重复、多顿时提示语带餐次而单顿时不带、
预算按整天不按顿（含"预算很紧时少排菜也不超预算"）、候选池切分（早餐菜不进晚餐池）、
LLM 提示词按餐给道数（单顿提示词一字不差）、**改动只动那一顿**（换一道/不做饭/改回来/回家晚了）。

### 第③步 ✅ 界面（2026-09-13）

**做了什么**

- 「排一周」表单多了一组「**吃哪几顿**」：多选（默认不选 = 只做晚餐），选中哪几顿就出现哪几个
  "N 个菜"控件；勾了多顿时多出"早餐单菜耗时上限"。原来的"每顿几个菜"滑杆**去掉了** ——
  留着就会出现两个互相打架的控件。
- 「本周计划」页：整周总览**每顿一行**（多顿时标出餐次，今天/已过的高亮按**天**算），
  每日详情的标签仍然是**一天一个**（"第 N 天"），页内再按餐分段。
- 「今晚」页：只做晚餐时还叫**今晚**（不能因为加了多餐就改名），多顿时叫**今天**；
  「回家晚了」「来客人了」「这道不吃」都只作用于**当前看的那一顿**。
- 服务端 DTO 补齐 `meals` / `dishes_per_meal` / `breakfast_max_time_min`（`ConstraintsOut` 与 `PlanCreateIn`）
  与 `client._record_from_detail` 的回读 —— 缺了它们，`USE_API=1` 时勾的餐次会被服务端**静默丢掉**。

**两个真 bug（都是这一步自己抓出来的）**

1. **`_dishes_per_meal()` 读错了键**：表单存进 `plan_inputs` 的是 `dishes_per_meal` 字典，
   而它读的是 `dishes_早餐` 这种**扁平键**（那只存在于控件的 session_state 里）→
   每餐道数全部回落成同一个值。用户选"早1/午2/晚3"会得到 3/3/3。**已修**，smoke 现在断言 1/2/3。
2. **数据库模式存不下多餐方案**：`plan_day` 现在是 `PRIMARY KEY(plan_id, day_no)`，
   一天多顿会直接撞唯一约束（`UNIQUE constraint failed`）。这正是第④步那次迁移要解决的，
   所以在迁移做完之前，界面**只在 JSON 存储下开放多餐**（`MEALS_AVAILABLE`）：
   数据库模式下控件是禁用状态并写明原因，绝不会让用户点出一个崩溃。

**⚠️ 一次事故与恢复（记录在案）**：第③步的诊断脚本忘了设 `DATABASE_URL`，
默认走到了**真实库** `data/app.db`，造出 2 版方案（plan 3→5、plan_day 9→18、plan_dish 18→36、
shopping_item 32→59、job 0→2）。发现后按先前核对过的原始状态逐表还原并逐表核对：
**plan 3 / plan_day 9 / plan_dish 18 / shopping_item 32 / shopping_check 0 / preference 1 /
rating 2 / job 0 / action_log 1，当前方案仍是 `b5ea0d22`**，与事故前完全一致。
教训与 `docs/07` §6 的老规矩同源：**临时脚本一律先设 `STORAGE=json` 或把 `DATABASE_URL` 指到 `.tmp/`**。

**验证**

| 套件 | 结果 |
|---|---|
| `pytest tests` | **237** 全绿 |
| `smoke_app.py` | JSON **183**（含新增的多餐用例 18 项）/ DB **163**（多餐按设计跳过） |
| `smoke_api_app.py` | **44** 全绿 |
| `self_check.py` | JSON **163** / DB **161** |

**下一步（第④步，现在是硬前提）**：Alembic `0002` 给 `plan_day` 加 `meal` 维度
（主键改 `(plan_id, day_no, meal)`，老数据填 `'晚餐'`）+ `repositories` 读写 `meal`
（`save_plan` / `_plan_to_record` / `_days_of` / `set_done`）+ `DayOut.meals` + 导出按餐分段
（顺带修 `reporting.py` 里写死 `dishes_per_day` 的那句"每顿 N 道菜"）。
**迁移做完才把 `MEALS_AVAILABLE` 放开到数据库模式。**

### 第④步 ✅ 数据库打通（2026-09-13）—— 多餐在两种存储下都能用

**⚠️ 设计修订（改动前先记下来）**：§3 原定把 `plan_day` 的主键改成 `(plan_id, day_no, meal)`。
实施时改成**只加列、不动主键**：

| | 原设计 | 实际做法 |
|---|---|---|
| 餐次落在哪 | `plan_day` 的主键加一维 + `plan_dish` 跟着改外键 | `plan_dish.meal` 一列（每道菜属于哪一顿） |
| "哪几顿不做饭 / 做过了" | 天然按顿（因为一天多行） | `plan_day.skipped_meals` / `done_meals` 两个短字符串 |
| 迁移代价 | **重建 plan_day 与 plan_dish 两张表** | 三次 `ADD COLUMN`（SQLite 上不重建表） |

理由：改主键要重建两张表，对一个**正在用的库**风险大得多；而餐次这个维度真正需要它的地方只有
"每道菜属于哪一顿"。"哪几顿"用两个短字符串（`'早餐,午餐'`）表达，配合 `PlanRecord.is_done(day, meal)`
在领域层补齐语义 —— 代价是「来客人了」的人数仍按**天**存（见"已知遗留"）。

**做了什么**

- `alembic/versions/0002_meals.py`：三次 `ADD COLUMN`（`plan_dish.meal` 用 `server_default '晚餐'`，
  老数据自动补，**不需要回填脚本**）；`orm` 同步加列（`tests/test_migrations.py` 逐表逐列比对两者）。
- `PlanRepo.save_plan` / `update_result`：按天归并（**一天仍然一行**）、`day_date` 改用 `day_no` 推算
  （原来用 `enumerate` 序号，多餐会串天）、菜的 `seq` 在一天内跨餐连续编号。
- `_plan_to_record`：把菜按 `meal` 分回多个 `DayPlan`（跳过/做过但没菜的顿也算），
  `done_slots` 从 `done_meals` 还原；`done_at` 这种老数据仍然当成"那天所有餐都做过了"。
- `set_done` 全链路加可选 `meal`：`store` / `db_store` / `PlanRepo` / `async_adapters` /
  API 的 `op=done`（`DayPatchIn.meal`）/ `client` / 界面「做完了」「再做一次」。
  不给 `meal` 就是老行为（记"这一天"），单顿时两者等价。
- `DayOut.meal`：一条 `DayOut` = 一天里的一顿（缺了它，API 读回来的多餐方案会**全部塌成晚餐**）。
- 放开 `MEALS_AVAILABLE`（0002 之后两种存储都支持多餐），老库给一条"先跑一次迁移"的提示。

**这一步修掉的真 bug**

1. `_picked_form` 在定义之前被引用 → 点场景卡直接崩（`UnboundLocalError`）。
2. `_dishes_per_meal` 读错了键（读 `dishes_早餐` 扁平键，而表单存的是 `dishes_per_meal` 字典）→
   每餐道数全部回落成同一个值。
3. 数据库模式存不下多餐（唯一约束）—— 本步的核心。

**顺带修的环境坑**

- 客户端两个 `httpx.Client` 加 `trust_env=False`：这台机器设了 `HTTP_PROXY`，实测连**不存在的域名**
  都会收到代理的 502 —— 也就是 `127.0.0.1` 也会被代理绕一圈。本地部署连本机服务不该走系统代理。
- `tests/api` 的 `wait_job` 轮询间隔 0.05 → 0.5 秒（20 次/秒几下就把 60/分钟的桶抽干）。
- 新增的存储测试不再调 `sync_bridge.shutdown()`（进程级常驻循环，停掉会让后面的任务/SSE 测试失效）。

**验证**

| 套件 | 结果 |
|---|---|
| `pytest tests` | **241** 全绿（新增 4 项数据库往返） |
| `smoke_app.py` | JSON **183** / DB **179**（多餐在数据库模式下也跑通）全绿 |
| `smoke_api_app.py` | **44** 全绿 |
| `self_check.py` | JSON **163** / DB **161** 全绿 |
| `verify_migration.py` | **18** 全绿（顺带把 8 道早餐菜谱 import 进了库） |
| 真机 `data/app.db` | 先备份再 `migrate` 0001→0002，逐表计数与迁移前**一致**，当前方案仍是 `b5ea0d22`，18 道老菜自动补成 `'晚餐'` |

**已知遗留（下一步）**

1. 「来客人了」的人数在库里仍按**天**一个值（存"最大的那个"）→ 按顿记人数。
2. ~~导出/打印里"每顿 N 道菜"那句还在用老字段 `dishes_per_day` → 按餐分段。~~
   **已在 §12 修掉**（`reporting.menu_title` / `dishes_phrase` / `_meal_mark`）。
3. `docs/10` §8 的四步全部完成；多餐功能到此可用（`docs/07` 的进度与坑清单待补这一轮的条目）。

### 第⑤步 ✅ 修「早中晚三餐显示成一样」（2026-09-13，用户报的 bug）

**用户报的现象**：勾了早/午/晚三餐，界面上三顿显示的是**同一份菜**。

**根因（一条，但性质严重）**：服务端详情接口 `GET /plans/{id}` 还在用

```python
def _day_plan(record, day):
    return next((p for p in record.result.days if p.day == day), None)   # ← 永远拿到早餐
```

`_dishes_of(record, day, db)` 也因此只按"天"取菜 → **三顿都返回当天第一顿（早餐）的菜**。
而 `minutes` / `cost` 来自 `summary.rows`（本来就按 (天, 餐) 分行）→ 于是界面出现
"菜一模一样、用时金额却各不相同"这种最能骗过肉眼的样子。库里、排菜内核里都是对的
（第②②③④步都验过），**只有读出来那一步丢了餐次**。

唯一没被覆盖到的原因：第②步替换了 `core` 里 7 处、第③步改了界面、第④步改了存储，
**唯独漏了 `api/routes/` 这一层** —— 而 §10 早就把这条写成了警告注释。

**做了什么**

- `api/routes/plans.py`：新增 `_slot()` / `_row_of()` / `_day_out()`，删掉 `_day_plan()`；
  详情页每一行按 `row.meal` 取自己那一槽；`GET /plans/{id}/days/{day}` 加 `meal` 查询参数
  （不给 = 当天最后一顿），`done` 改用 `record.is_done(day, meal)`。
- **单点改动全线带餐次**（`api/routes/plan_mutations.py` + `actions.py`）：
  `skip` / `restore` / `people` / `faster` / `swap` / `replace_day` / `done` / `feedback` / `rate`
  都加了可选 `meal`；`DayPatchIn.meal` 从"只对 op=done 有效"变成**对所有 op 有效**；
  `FeedbackIn.meal` / `RateIn.meal` 新增；`_day_payload()` 按 (天, 餐) 回执；
  撤销线索 `_replace_day_undo()` 也点名餐次（否则"撤销今晚换的一道"会撤到早餐上）。
- 回执文案统一走 `core.where_text()`：**只做一顿时一字不改**（"第 3 天"），
  多顿时变"第 3 天午餐"。校验消息的 `第3天早餐` 老措辞（有测试压着）走同一个函数的 `spaced=False`。
- 候选池按餐取齐：`cheapest_swap` / `_swap_by_rule`（省钱 / 太油腻 / 别太素）
  改成"最贵那道在哪一顿，就在**那一顿**的池子里找替代"，返回值多一个 `meal`；
  `candidates_matching` 支持 `meal`（"早餐来个鱼"不会排上 55 分钟的硬菜）。
- `client._day_patches`：按 **(天, 餐)** 比对，并给多餐那天的操作带上 `meal`；
  **单餐时请求体与以前一字不差**（只在当天真的有多顿时才加字段）。
- `PlanDetailOut.done_slots`：接口把"哪一顿做完了"还给客户端并还原成 `PlanRecord.done_slots`
  （原来只回 `done_days`，"做完了早餐"刷新一次就丢）。
- 导出/打印按餐分段：`menu_title()`（只做晚餐仍叫"一周晚餐菜单"，多餐才叫"本周菜单"）、
  `dishes_phrase()`（多餐时"早餐 1 道、午餐 2 道、晚餐 3 道"）、`_meal_mark()`（打印/分享里标出餐次）。

**新增的三道防线**（这个 bug 靠"记得别这么写"是防不住的）

1. `tests/test_meal_slot_guard.py`：**AST 源码守卫** —— 全项目扫描
   `next(<生成器，来源是 .days/.rows，条件里只比较 .day 不比较 .meal>)`，命中就失败；
   例外要写 `# noqa: meal-slot` 并说明理由。守卫自己也有一条用例（拿当初出事的那一行喂给它）。
2. `tests/api/test_meals_api.py`（10 项）：直接断言**三餐的菜真的不一样**，
   以及 换一道 / 不做饭 / 做完了 / 打分 / 定住 各自只落在点名的那一顿上。
3. `tests/test_client.py` 新增 4 项：多餐按顿比对、详情 → 领域对象的餐次回读。

**顺带修掉的假警报**：`verify_migration.py` 的"方案条数一致"在用户随手多排两版后必然变红 ——
它比的是"JSON 迁移基线"和"现在的库"。改成校验**"JSON 里每一份方案都还在库里"**
（迁移没丢东西），库里多出来的新方案会被点名打印、不参与比对。

**验证**

| 套件 | 结果 |
|---|---|
| `pytest tests` | **257** 全绿（241 → +16：接口 10 / 守卫 2 / 客户端 4） |
| `smoke_app.py` | JSON **183** / DB **179** 全绿 |
| `smoke_api_app.py` | **44** 全绿 |
| `self_check.py` | JSON **163** / DB **161** 全绿 |
| `verify_migration.py` | **18** 全绿 |
| 真机服务端 | 重启后把用户那份三餐方案逐顿打出来：早餐 燕麦牛奶+小米南瓜粥 / 午餐 蚝油生菜+清炒油麦菜 / 晚餐 白灼菜心+香菇青菜 —— **三顿各不相同**，同餐重复天数 0 |

**这一轮的观察（不是 bug，先记下）**：那份方案的午/晚大多是素菜，原因是用户自己的约束
（3 人 · ¥40/人·天 · 一天 6 道 · 忌海鲜 · 控糖清淡）—— ¥120/天 ÷ 6 道 ÷ 1.5 倍份量系数
≈ 每道 ¥13 预算，荤菜基本进不来。属于"预算算得对"，不是排菜出错。

**还没做的（等确认）**：「今晚/今天」页在多餐时仍然只展示**当天最后一顿**（一般是晚餐），
虽然标题已经改成「今天」。要不要改成"一屏列出今天三顿"是个界面设计选择，见对话中的提问。

### 第⑥步 ✅ 「今天」页一天列出三顿（2026-09-13，用户确认后做的）

上一步留下的那个界面选择，用户说"可以"，于是按提的**一顿一张卡**做：从早到晚依次往下排，
每张卡各带自己的状态与快改按钮。

**做了什么**

- `app.py`：把原来长在 `render_tonight()` 里的整段渲染抽成 `_tonight_slot(view, ...)`
  —— 主角卡 + 每道菜 + 这一顿的快改 + 下锅顺序 + 放宽选项。
  单餐时这一页仍然只调用它一次，**控件 key 与文案一字不变**；多餐时一天调用三次。
  周级状态（`no_plan` / `week_over`）抽成 `_tonight_week_over()`，多餐时也只画一次。
- 控件 key 加餐次后缀（`quick_done_早餐`）：同一页三个"做完了"不区分餐次，
  Streamlit 会直接**报控件 key 重复**。`guests_for` / `show_order_for` / `tonight_relax`
  这几个界面状态也从"第几天"改成 `_slot_tag(day, meal)` 元组，否则一顿的操作会串到另一顿。
- 顺手修掉同一个病根的两处：
  1. **`TonightOut` 少了 `meal`** —— `tonight_view.to_dict()` 一直在给，DTO 没这个字段就被
     静默丢掉，客户端也不回读 → `USE_API=1` 时界面永远以为自己在看晚餐，
     "做完了 / 来客人了 / 改回来做"**全落到晚餐上**。现已补齐 `meal`（含两个 tonight 接口的
     `meal` 查询参数与 `client.tonight_view` 的入参和回读）。
  2. **「看看明天」算错天**：`view.day % len(result.days) + 1` 里的 `len(result.days)` 是**顿数**，
     一天三顿时第 3 天会跳到第 4 天。改用 `days_span_of()`（天数），并在"排好了"的回执里
     把"排好 N 天晚餐"也改成按餐次说。
- `tonight.py`：「18:30 开始做」**只对当天最后一顿**说 —— 否则三张卡都写这句话，
  而早上八点不会开始做晚饭。单餐时这条规则不生效（老行为一字不变）。

**验证**

| 套件 | 结果 |
|---|---|
| `pytest tests` | **265** 全绿（257 → +8：tonight 单元 4 / 接口 2 / 客户端 2） |
| `smoke_app.py` | JSON **186**（新增 3 项多餐用例）/ DB **182** 全绿 |
| `smoke_api_app.py` / `self_check.py` | **44** / JSON 163 · DB 161 全绿 |
| `verify_migration.py` | **18** 全绿 |
| 真机服务端 | `/plans/current?day=1&meal=早/午/晚` 三次请求各回各的菜（燕麦牛奶·粥 / 蚝油生菜·油麦菜 / 白灼菜心·青菜），餐次回读正确，不给 meal 时是当天最后一顿 |

新增的检查里有一条是专门盯这次的坑：`smoke_app.py` 断言"今天页把这一天的**三顿一次画全**
（每顿都是自己那几道菜）"—— 只画一顿、或者三张卡是同一份菜，它都会红。

### 第⑦步 ✅ 「本周计划」改成卡片：一天一段，段内早/午/晚各一张卡（2026-09-13）

用户要的："本周计划也像主页一样做成卡片，一天里的早中晚"。方案先在对话里确认
（"竖着全展开"＋"整周总览收进折叠"），再做。

**改之前的这一页**：顶上一排 `.day-row` 天行，下面「每日详情」折叠里**一天一个标签页**，
页内再按餐分段（每餐一个小标题 + 每道菜一张大卡）。

**改之后**：

- **主体 = 一天一段**：段头 `第 3 天 周三 9/16` ＋ `今天`/`已过` 徽标 ＋ 右边一句摘要；
  段内 **早/午/晚各一张餐卡**，竖着一路看下来，不再需要点标签。
- **每张餐卡**（`st.container(border=True, key="mealcard_{天}_{餐}")`）：卡头是餐次 ＋
  `约 23 分钟 · ¥16`（**这一顿自己的**），卡身是这一顿的菜（每道菜一行 + 那四个按钮
  换一道/定住/喜欢/不喜欢），卡脚是「下锅顺序」。
- **不做饭的那一顿**：卡片里就一句"这顿不做饭（你标记过：不计花费、也不进买菜清单）"＋
  「改回来做」—— 与「今天」页的说法一致。
- **整周一览**：原来平铺在顶部的天行列表收进折叠（`整周一览（扫一眼七天）`），
  扫七天用它、逐顿细看用卡片。
- **只做晚餐时**：一天一段、段里一张卡，**段头措辞与以前一样**（`合计约 X 分钟 · ¥Y`）；
  多餐时改说 `3 顿 · 合计 ¥45` —— 三顿饭的做菜时间加起来说"合计 89 分钟"是误导。
- **样式**（`app.py` 的样式表，取值仍按 `06`）：`.day-head` / `.day-badge` / `.meal-head` /
  `.meal-name` / `.meal-meta` / `.meal-tag`；餐卡里的 `.dish-card` 去掉自己的外框
  （框由整张卡提供），只留一条虚线分隔，避免"卡中卡"。
- **一起消失的坑**：一天一个标签页那套（含"标签选中态越界要纠正"）没有了 ——
  天数变少时不再有"停在不存在的第 N 天"这种状态。

**⚠️ 第三次数据事故（这次是 JSON 存档，已按字节还原）**：为了肉眼看新结构，我写了个
诊断脚本用 AppTest 渲染 `app.py`；它设了 `STORAGE=json` 就以为安全，但 `app.py` **真的会生成
并保存一份方案** —— 于是写进了**真实的** `data/saved_plans.json`。那份存档只留最近 3 份
（`store.MAX_PLANS`），新方案把迁移基线里最老的一版 `1bbf04e0` 挤掉了，
`verify_migration.py` 立刻变红（"JSON 里每一份方案都还在库里"失败）——**这条检查值了**。
从 `data/backup/20260913-204312/saved_plans.json` 按 SHA256 校验后原样还原
（17047 字节、mtime 09-11 18:48:33、3 份方案 = b5ea0d22 / 4914864b / 1bbf04e0），
`verify_migration` 回到 18/0。**数据库一个字节都没动**（事故只在 JSON 存档里）。

**这次不只是写进文档，而是加了守卫**：`scripts/isolate_tmp.py` —— 一句话
`isolate()` 把**三种**存储（`DATABASE_URL` / `RECIPE_PLAN_FILE` / `RECIPE_PROFILE_FILE`）
全部指到 `.tmp/`，并且**"没设"也算不安全**（第③步、第⑦步两次事故分别是"没设 DATABASE_URL"
和"没设另外两个"）。`tests/test_isolate_tmp.py` 5 条用例守着它本身：故意只设 `STORAGE`
必须被拦下、指向 `data/` 必须被拦下、`isolate()` 之后三个落点都在 `.tmp/`，
最后一条**只读检查真实存档**（3 份、没被截断）。诊断脚本现在跑完，真实存档的 SHA256 不变。

**验证**

| 套件 | 结果 |
|---|---|
| `pytest tests` | **270** 全绿（265 → +5：隔离守卫 5） |
| `smoke_app.py` | JSON **189** / DB **185** 全绿（[8]/[12]/[14]/[21] 四组按新结构重写） |
| `smoke_api_app.py` / `self_check.py` | **44** / JSON 163 · DB 161 全绿 |
| `verify_migration.py` | **18** 全绿 |
| 真实存档 | SHA256 `FBF016FA…2EF22B` 与还原备份一致，跑完所有用例**未变** |

**顺带修掉一个"按时间才会红"的测试**：`test_manual_day_ignores_numbers_outside_the_plan`
只注入了 `today` 没注入 `now`，于是"过了 22:00 先看明天"那条规则会拿真实当前时间判断 ——
白天绿、22 点后红。现在 `now` 也注入（这正是它今晚变红的原因）。





