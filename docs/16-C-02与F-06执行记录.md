# 16 · C-02 与 F-06 执行记录

> 更新：2026-09-20 ｜ 本文记录 `15` 执行后的代码与数据验收，不覆盖 `13` 的历史复核快照。

## C-02 菜谱数据库主源：已完成核心闭环

`data/recipes.json` 仍保留为 seed、审查快照、fixture 和备份格式；运行时默认只读取 SQLite 中
`status=published` 的菜谱。2026-09-20 已将本机数据库从迁移 `0004` 升到 `0006`，并把 127 道菜谱
导入为已发布内容。原有方案、方案日、方案菜、采购项、偏好、评分和逐菜事件记录没有被重建或清空。

实现内容：

- `0005_recipe_catalog_fields`：碳水/脂肪、状态、版本、来源、审核、营养元数据、内容哈希、批次号和创建时间；
- `0006_recipe_references`：可扩展的 video/article/search 引用表，并兼容迁移旧 `video_url`；
- `RecipeRepo`：published 候选池、内容哈希幂等导入、内容版本递增、审核批次发布、归档和快照导出；
- `recipe_planner.content`：`import_batch`、`publish_batch`、`archive`、`export_snapshot`、`seed`；
- API 菜谱 DTO 返回步骤、食材克数、营养字段、引用和版本；`USE_API=1` 的界面通过 `/api/v1/recipes`
  读取目录，不再由界面进程读取 `data/recipes.json`；
- 同批导入在单个数据库事务中执行，审核状态不会进入推荐候选池。

验收结果：

- 本机 `recipe`：127 道，`published`：127 道；
- Alembic 当前版本：`0006`；
- 数据库快照可以导出并再次作为内容批次校验输入；
- 迁移、schema、仓储、API 和完整 pytest 回归通过。

## F-06 Windows 审计输出：已完成

`scripts/audit_live_data.py` 的最终状态标记改为 ASCII `[OK]` / `[ERROR]`，并增加 GBK 子进程回归测试。
脚本仍只读真实数据；`scripts/verify.ps1` 现在也将真实数据审计作为门禁步骤。

## 后续仍未完成

- `13` 的 A6 目标机器人工验收仍需用户实际打开网站并完成五步记录；
- API 正式部署条件 B-01～B-07 仍按 `15` 保持条件分支；
- M-01 继续等待至少 4 周真实使用数据，当前不能据此宣称推荐效果趋势。
